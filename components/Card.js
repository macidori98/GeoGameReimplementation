import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Card = props => {
  return (
    <View>
      <Text>Card</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Card;
