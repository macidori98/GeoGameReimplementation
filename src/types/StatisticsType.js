/**
 * @typedef {{correctAns: number, date: string, time: string, duration: string}} StatisticsData
 */

/**
 * @typedef {{correctAnswer: string, givenAnswer: string, question: string, questionEnding: string}} QuestionStatistic
 */

/**
 * @typedef {{data: StatisticsData, questions: QuestionStatistic[], type: string}} StatisticsDataWithQuestions
 */
